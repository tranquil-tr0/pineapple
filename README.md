# Pizza

A self-hostable web interface for the Pi coding agent.

## Screenshots


## Setup

```
bun install
bun run dev
```

`bun run dev` runs the server and client together with hot reload, so changes to source files take effect immediately. It works on Windows, macOS, and Linux.

For production use, build and run separately:

```
bun run build
bun start
```
